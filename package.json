{
  "name": "envdiff",
  "version": "1.0.0",
  "description": "Utility that compares .env files across environments and highlights missing or mismatched keys",
  "main": "src/index.js",
  "bin": {
    "envdiff": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test": "jest"
  },
  "keywords": ["env", "dotenv", "diff", "compare", "environment"],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
